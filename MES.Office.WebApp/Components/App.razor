<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    <!-- Fonts -->
    <link href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet" />
    <!-- MudBlazor (primary UI framework) -->
    <link href="_content/MudBlazor/MudBlazor.min.css" rel="stylesheet" />
    <!-- Application styles -->
    <link rel="stylesheet" href="app.css" />
    <link rel="stylesheet" href="MES.Office.WebApp.styles.css" />
    <link rel="icon" type="image/png" href="favicon.png" />
    <HeadOutlet @rendermode="InteractiveServer" />
</head>

<body>
    <Routes @rendermode="InteractiveServer" />

    <!-- Custom Blazor Server Reconnection UI -->
    <div id="components-reconnect-modal" class="components-reconnect-hide">
        <div class="reconnect-overlay">
            <div class="reconnect-dialog">
                <div class="reconnect-content">
                    <div class="reconnect-icon">
                        <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M21 12a9 9 0 0 0-9-9 9.75 9.75 0 0 0-6.74 2.74L3 8"/>
                            <path d="M3 3v5h5"/>
                            <path d="M3 12a9 9 0 0 0 9 9 9.75 9.75 0 0 0 6.74-2.74L21 16"/>
                            <path d="M16 21h5v-5"/>
                        </svg>
                    </div>
                    <h3 class="reconnect-title">Reconnecting to Server</h3>
                    <p class="reconnect-message">Please wait while we restore your connection...</p>
                    <div class="reconnect-spinner"></div>
                </div>
                <div class="reconnect-failed" style="display: none;">
                    <h3 class="reconnect-title">Connection Lost</h3>
                    <p class="reconnect-message">Unable to reconnect to the server. This may be because:</p>
                    <ul class="reconnect-reasons">
                        <li>The API server is not running</li>
                        <li>Your network connection was interrupted</li>
                        <li>The server is restarting</li>
                    </ul>
                    <button class="reconnect-reload-btn" onclick="location.reload()">
                        Reload Page
                    </button>
                    <p class="reconnect-dev-hint">
                        <strong>Developer Mode:</strong> If testing UI without API, you can continue browsing.
                        Some features may not work until the API is available.
                    </p>
                </div>
            </div>
        </div>
    </div>

    <style>
        /* Reconnect Modal Styles */
        .components-reconnect-hide {
            display: none;
        }

        .components-reconnect-show .reconnect-overlay,
        .components-reconnect-failed .reconnect-overlay,
        .components-reconnect-rejected .reconnect-overlay {
            display: flex;
        }

        .components-reconnect-failed .reconnect-content,
        .components-reconnect-rejected .reconnect-content {
            display: none;
        }

        .components-reconnect-failed .reconnect-failed,
        .components-reconnect-rejected .reconnect-failed {
            display: block !important;
        }

        .reconnect-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.6);
            z-index: 9999;
            justify-content: center;
            align-items: center;
            backdrop-filter: blur(4px);
        }

        .reconnect-dialog {
            background: white;
            border-radius: 12px;
            padding: 32px;
            max-width: 420px;
            width: 90%;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            text-align: center;
        }

        .reconnect-icon {
            color: #1976d2;
            margin-bottom: 16px;
        }

        .reconnect-title {
            font-size: 1.5rem;
            font-weight: 500;
            margin: 0 0 12px 0;
            color: #333;
        }

        .reconnect-message {
            color: #666;
            margin: 0 0 20px 0;
            font-size: 0.95rem;
        }

        .reconnect-spinner {
            width: 40px;
            height: 40px;
            border: 3px solid #e0e0e0;
            border-top-color: #1976d2;
            border-radius: 50%;
            animation: reconnect-spin 1s linear infinite;
            margin: 0 auto;
        }

        @@keyframes reconnect-spin {
            to { transform: rotate(360deg); }
        }

        .reconnect-reasons {
            text-align: left;
            color: #666;
            font-size: 0.9rem;
            margin: 16px 0;
            padding-left: 24px;
        }

        .reconnect-reasons li {
            margin: 6px 0;
        }

        .reconnect-reload-btn {
            background: #1976d2;
            color: white;
            border: none;
            padding: 12px 32px;
            border-radius: 6px;
            font-size: 1rem;
            cursor: pointer;
            margin-top: 16px;
            transition: background 0.2s;
        }

        .reconnect-reload-btn:hover {
            background: #1565c0;
        }

        .reconnect-dev-hint {
            margin-top: 20px;
            padding: 12px;
            background: #fff3e0;
            border-radius: 6px;
            font-size: 0.85rem;
            color: #e65100;
        }
    </style>

    <script src="_framework/blazor.web.js"></script>
    <script src="_content/MudBlazor/MudBlazor.min.js"></script>
</body>

</html>
